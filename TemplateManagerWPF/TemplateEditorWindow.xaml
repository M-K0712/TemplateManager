<Window x:Class="TemplateManagerWPF.TemplateEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="定型文編集" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル入力 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="タイトル:" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="TitleTextBox" FontSize="14" Padding="5"/>
        </StackPanel>

        <!-- セクション入力 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="セクション:" FontSize="14" Margin="0,0,0,5"/>
            <ComboBox x:Name="SectionComboBox"
                      IsEditable="True"
                      FontSize="14"
                      Padding="5"/>
        </StackPanel>

        <!-- 概要入力 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="概要:" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="SummaryTextBox"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>

        <!-- 本文入力 -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="本文:" FontSize="14" Margin="0,0,0,5"/>
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
                <TextBox x:Name="BodyTextBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         Padding="5"/>
            </Border>
        </Grid>

        <!-- ボタン -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="SaveButton"
                    Content="保存"
                    Width="100"
                    Padding="10,5"
                    Margin="0,0,10,0"
                    Click="SaveButton_Click"/>
            <Button x:Name="CancelButton"
                    Content="キャンセル"
                    Width="100"
                    Padding="10,5"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
