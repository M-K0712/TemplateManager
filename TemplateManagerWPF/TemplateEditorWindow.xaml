<Window x:Class="TemplateManagerWPF.TemplateEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemplateManagerWPF"
        mc:Ignorable="d"
        Title="定型文登録" Height="650" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="タイトル:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="TitleTextBox" Padding="5" FontSize="14"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="セクション（複数選択可）" Margin="0,0,0,15" Padding="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="120"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox x:Name="ExistingSectionsListBox" Margin="0,0,0,5" BorderBrush="#DDD">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" 
                                      IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                      Margin="3" VerticalContentAlignment="Center"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewSectionTextBox" Padding="5" VerticalContentAlignment="Center" />
                    <Button Grid.Column="1" Content="新規追加" Width="70" Margin="5,0,0,0" Click="AddSection_Click" Padding="5"/>
                </Grid>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="概要（リスト表示用）:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="SummaryTextBox" Padding="5" FontSize="14"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="本文:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox Grid.Row="1" x:Name="BodyTextBox" 
                     AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" Padding="5" FontSize="14"/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="保存" Width="100" Height="35" IsDefault="True" Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button x:Name="CancelButton" Content="キャンセル" Width="100" Height="35" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>