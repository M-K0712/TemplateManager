<Window x:Class="TemplateManagerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemplateManagerWPF"
        xmlns:viewmodels="clr-namespace:TemplateManagerWPF.ViewModels"
        mc:Ignorable="d"
        Title="定型文マネージャー" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewmodels:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="検索:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding SearchTargets}" SelectedItem="{Binding SearchTarget}" Margin="0,0,5,0" Padding="5"/>
                <TextBox Grid.Column="2" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Padding="5" VerticalContentAlignment="Center"/>
                <Button Grid.Column="3" Command="{Binding SearchCommand}" Content="検索" Width="80" Margin="0,0,5,0" Padding="10,5"/>
                <Button Grid.Column="4" Command="{Binding ShowAllCommand}" Content="全解除" Width="80" Padding="10,5"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Command="{Binding AddTemplateCommand}" Content="新規登録" Width="100" Margin="0,0,5,0" Padding="5"/>
                <Button Command="{Binding EditTemplateCommand}" Content="編集" Width="80" Margin="0,0,5,0" Padding="5"/>
                <Button Command="{Binding DeleteTemplateCommand}" Content="削除" Width="80" Padding="5"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Background="#F9F9F9">
                        <TextBlock Text="セクション" Margin="8,8,8,4" FontWeight="Bold" FontSize="14"/>
                        <CheckBox IsChecked="{Binding IsAndFilter}" 
                      Margin="8,0,8,8" 
                      Cursor="Hand">
                            <TextBlock Text="選択した条件を全て満たす" FontSize="11" VerticalAlignment="Center"/>
                        </CheckBox>
                        <Separator Background="#DDD" Margin="0"/>
                    </StackPanel>

                    <ListBox ItemsSource="{Binding SectionFilters}" 
                 BorderThickness="0"
                 HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" 
                              IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                              Margin="5,3"
                              FontSize="13"
                              VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent"/>

            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1">
                <ListBox ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                                <ItemsControl ItemsSource="{Binding Sections}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding StringFormat='[{0}] '}" Foreground="Blue" FontSize="11"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="{Binding Summary}" Foreground="Gray" FontSize="12"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <GridSplitter Grid.Column="3" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent"/>

            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="本文（一時編集可能）:" Margin="0,0,0,5" FontSize="14"/>
                <TextBox Grid.Row="1" 
                         Text="{Binding EditableBody, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto" 
                         Padding="5"/>
                <Button Grid.Row="2" 
                        Command="{Binding CopyToClipboardCommand}" 
                        Content="クリップボードにコピー" 
                        Height="40" 
                        Margin="0,10,0,0"/>
            </Grid>
        </Grid>

        <StatusBar Grid.Row="2" Margin="0,5,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>